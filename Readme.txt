1.data_processing.py
此文件用于原始VHR数据的预处理，包括以下流程：
1.读入数据，以collectTime为基准转换时间格式并排序
2.异常数据处理（尝试了Z-score、IQR；分别删除10436行数据及14209行“异常数据”明显不正确）
3.缺失数据填充
2.data_ana.py（用于分析哪些数据被判定为了异常值导致误删）
输出5个删除行最多的特征
发现有些数据至少75%为0，属于严重偏差数据

修改data_processing.py
最后采用Q10-Q90区间，减少了数据删除量，一共删除2506行
但在90%都是0的情况下，就算数据合理也会被删除


/////
1.首先读入数据，数据要按照collectTime准备排序

2.我已检查从14列开始是传感器特征数据，滑动窗口设置为60s，每10s滑动一次，然后计算窗口内所有特征的均值，标准差、最大值最小值

3.使用PCA进行降维处理，返回一共降到了几维，如果能够有相关绘图可以加入

4.基于PCA降维后的特征进行聚类学习，加入前面所提到的剪影系数来找出最佳K值

5.聚类后如果有任何可以可视化的手段，请将聚类结果进行可视化

6.反向映射，将聚类标签添加到没有做PCA但已经做过特征工程计算的样本上

7.将不同类别的样本数据分开，保存在一个新的文件，最前面加上一列category_id来区分样本的不同类别。
/////

3.feature_calculate.py
特征工程，在滑动窗口内计算所有特征的均值，标准差，最大最小值并保存为新的文件
4.run_cluster.py
对计算的特征值进行PCA降维
分别计算降维维度N，聚类算法（k-means，GMM），以及聚类个数K在什么时候出现最好的聚类效果，选用相应的选项
为经过特征工程计算的数据打上聚类标签
5.llmbased_labled.py
利用预定义态势的相关字段保存每个类别的相应信息，并交给LLM进行态势分类并将标签标给预处理后的原始数据


